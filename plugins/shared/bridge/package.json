{
  "name": "@toolbox/bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Exposes electronAPI for plugins running in Electron webview — preload injected directly, no postMessage bridge needed",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "devDependencies": {
    "electron": "^33.0.0",
    "typescript": "^5.7.0"
  }
}
